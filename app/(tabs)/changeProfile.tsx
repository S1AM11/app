import { ThemedText } from "@/components/ThemedText";
import { ThemedView } from "@/components/ThemedView";

const ChangeProfile = () => {
  return (
    <ThemedView>
      <ThemedText> this is Change profile page</ThemedText>
    </ThemedView>
  );
};
export default ChangeProfile;
