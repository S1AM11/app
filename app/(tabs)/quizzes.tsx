import { ThemedText } from "@/components/ThemedText";
import { ThemedView } from "@/components/ThemedView";
import { Text, View } from "react-native";

const Quizzes = () => {
  return (
    <ThemedView>
      <ThemedText> this is quizze page</ThemedText>
    </ThemedView>
  );
};

export default Quizzes;
